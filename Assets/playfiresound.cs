using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class playfiresound : MonoBehaviour
{

    public AudioSource fireSpit;

    public void play() {
        fireSpit.Play();
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
